pub struct metrics {
    pub point_of_sail: String,
    pub optimal_sail_angle: f64,
    pub trim_score: u8,
    pub speed_factor: f64,
    pub notes: String,
}

fn main(){
    println!("Hello, world!");
}